__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
run.log

# Power BI 專案本機快取／自動產生檔（Desktop 產生，不入版控）
**/.pbi/
**/.platform
**/diagramLayout.json

# 修改前的備份檔（*.bak 系列），僅本機留存
*.bak
*.bak[0-9]

# 保留 data/raw 與 data/curated：方案 B 以 git 版本歷史作為稽核軌跡與資料累積。

# 歷史彙整表 PDF 原始檔（150 檔、體積大、屬公開資料）保留本機、不推上 GitHub；
# 由其轉出的 data/history/material_price_history.csv 則入庫。
data/history/pdf/

# 價格趨勢 PDF 原始檔（fetch_pdf 下載的 6 系列、約 900 檔、體積大、屬公開資料）
# 保留本機作為 raw 封存，不推 GitHub；遺失可隨時以 src/fetch_pdf.py 重抓。
# 僅 _manifest.json（下載清單/雜湊）入庫。轉出的 history CSV 亦入庫。
data/raw/pdf/**/*.pdf
